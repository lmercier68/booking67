export default function save( { attributes } ) {
	return (
		<div className="booker67-datepicker-container">
			{/* Ceci est un élément HTML standard. Vous utiliserez du JS en frontend pour le transformer en datepicker. */}
			<input type="text" id="booker67-datepicker" name="booker67-datepicker" placeholder="Sélectionnez une date" />
		</div>
	);
}
