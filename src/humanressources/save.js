export default function save( { attributes } ) {
	return (
		<div className="booking67-datepicker-container">
			{/* Ceci est un élément HTML standard. Vous utiliserez du JS en frontend pour le transformer en datepicker. */}
			<input type="text" id="booking67-datepicker" name="booking67-datepicker" placeholder="Sélectionnez une date" />
		</div>
	);
}
